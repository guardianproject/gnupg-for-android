package info.guardianproject.gpg.screens;

import info.guardianproject.gpg.R;
import info.guardianproject.gpg.Constants;
import android.app.Activity;
import android.os.Bundle;
import android.view.View;
import android.view.View.OnClickListener;

public class WebOfTrustActivity extends Activity implements Constants, OnClickListener {
	@Override
	public void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.weboftrustactivity);
	}

	@Override
	public void onClick(View v) {
		
		
	}
}
